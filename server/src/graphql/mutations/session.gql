mutation CreateSession($session: SessionInput, $destroy: Boolean) {
	session(session: $session, destroy: $destroy) {
		jwt
		user {
			id
			name
			url
		}
		character {
			id
			name
			url
			images {
				path
			}
		}
		campaign {
			id
			name
			url
		}
	}
}
