mutation CreateSession($session: SessionInput!) {
	session(session: $session) {
		jwt
		user {
			id
			username
			url
		}
		character {
			id
			name
			url
		}
		campaign {
			id
			name
			url
		}
	}
}
